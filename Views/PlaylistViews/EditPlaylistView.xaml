<UserControl x:Class="NetEase.Views.PlaylistViews.EditPlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetEase.Views.PlaylistViews"
             xmlns:behaviors="clr-namespace:NetEase.Behaviors" 
             xmlns:playlistviewmodels="clr-namespace:NetEase.ViewModels.PlaylistViewModels" 
             d:DataContext="{d:DesignInstance Type=playlistviewmodels:EditPlaylistViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <UserControl.Resources>
        <BitmapImage x:Key="DefaultAvatarImage"
                    UriSource="/CoverImage/39.jpg"/>

        <Style x:Key="ModernDataGridStyle"
              TargetType="{x:Type DataGrid}">
            <!-- 基础属性设置 (从您原来的 DataGrid 标签上移到这里) -->
            <Setter Property="AutoGenerateColumns"
                   Value="False"/>
            <Setter Property="IsReadOnly"
                   Value="True"/>
            <Setter Property="CanUserResizeColumns"
                   Value="True"/>
            <Setter Property="CanUserReorderColumns"
                   Value="False"/>
            <Setter Property="GridLinesVisibility"
                   Value="None"/>
            <Setter Property="HeadersVisibility"
                   Value="Column"/>
            <Setter Property="Background"
                   Value="Transparent"/>
            <Setter Property="BorderThickness"
                   Value="0"/>
            <Setter Property="RowHeaderWidth"
                   Value="0"/>
            <Setter Property="AlternationCount"
                   Value="2"/>
            <Setter Property="HorizontalScrollBarVisibility"
                   Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGrid}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               Background="{TemplateBinding Background}"
                               Padding="{TemplateBinding Padding}"
                               SnapsToDevicePixels="True">

                            <!-- 
                       找到 DataGrid 内部的 ScrollViewer (它的名字是 PART_RowsPresenter)，
                       并将它的 Style 设置为我们的 ModernScrollViewerStyle。
                   -->
                            <ScrollViewer x:Name="PART_RowsPresenter"
                                         Style="{StaticResource for_scrollviewer}"
                                         Focusable="False">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================== 2. 自定义 TabControl 样式 ================== -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <!-- 输入框样式 -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#3D3D40"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollViewer"
              TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!-- 内容呈现 -->
                            <ScrollContentPresenter Grid.Column="0"
                                                   Grid.Row="0"
                                                   CanContentScroll="{TemplateBinding CanContentScroll}"
                                                   Content="{TemplateBinding Content}"/>
                            <!-- 垂直滚动条 -->
                            <ScrollBar Grid.Column="1"
                                      Grid.Row="0"
                                      Name="PART_VerticalScrollBar"
                                      Orientation="Vertical"
                                      Maximum="{TemplateBinding ScrollableHeight}"
                                      ViewportSize="{TemplateBinding ViewportHeight}"
                                      Value="{TemplateBinding VerticalOffset}"
                                      Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <!-- 水平滚动条 -->
                            <ScrollBar Grid.Column="0"
                                      Grid.Row="1"
                                      Name="PART_HorizontalScrollBar"
                                      Orientation="Horizontal"
                                      Maximum="{TemplateBinding ScrollableWidth}"
                                      ViewportSize="{TemplateBinding ViewportWidth}"
                                      Value="{TemplateBinding HorizontalOffset}"
                                      Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                            <!-- 右下角填充（可选） -->
                            <Rectangle Grid.Column="1"
                                      Grid.Row="1"
                                      Fill="LightGray"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- 自定义 ScrollViewer 的额外属性 -->
            <Setter Property="Background"
                   Value="White"/>
            <Setter Property="BorderBrush"
                   Value="Gray"/>
            <Setter Property="BorderThickness"
                   Value="1"/>
        </Style>

        <Style x:Key="ActionButtonStyle"
              TargetType="Button">
            <Setter Property="Foreground"
                   Value="White"/>
            <Setter Property="BorderThickness"
                   Value="1"/>
            <Setter Property="BorderBrush"
                   Value="#555555"/>
            <Setter Property="Background"
                   Value="#333333"/>
            <Setter Property="Padding"
                   Value="8,8"/>
            <Setter Property="Margin"
                   Value="0,0,10,0"/>
            <Setter Property="Cursor"
                   Value="Hand"/>
            <Setter Property="Height"
                   Value="40"/>
            <Setter Property="Width"
                   Value="80"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PlayAllButtonStyle"
              BasedOn="{StaticResource ActionButtonStyle}"
              TargetType="Button">
            <Setter Property="Background"
                   Value="#EC4141"/>
            <Setter Property="BorderBrush"
                   Value="Transparent"/>
        </Style>

        <Style x:Key="CustomColumnHeaderStyle"
              TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background"
                   Value="Transparent"/>
            <Setter Property="Foreground"
                   Value="#888888"/>
            <Setter Property="BorderThickness"
                   Value="0,0,1,1"/>
            <Setter Property="BorderBrush"
                   Value="#333333"/>
            <Setter Property="Padding"
                   Value="10,5"/>
            <Setter Property="HorizontalContentAlignment"
                   Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                        <Grid>
                            <!-- 1. 正常状态下显示的内容 -->
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   Background="{TemplateBinding Background}"
                                   Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                 VerticalAlignment="Center"/>
                            </Border>

                            <!-- 2. 悬浮时才显示的内容 (初始不可见) -->
                            <Border x:Name="HoverContent"
                                   Background="#3A3A3B"
                                   Padding="{TemplateBinding Padding}"
                                   CornerRadius="4"
                                   Opacity="0">
                                <StackPanel Orientation="Horizontal">
                                    <!-- 排序图标 -->
                                    <TextBlock Text="&#xE8CB;"
                                              FontFamily="Segoe MDL2 Assets"
                                              Foreground="#CCCCCC"
                                              VerticalAlignment="Center"
                                              Margin="0,0,5,0"/>
                                    <!-- 排序文字 -->
                                    <TextBlock Text="默认排序"
                                              Foreground="#CCCCCC"
                                              VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- 当鼠标悬浮在列标题上时 -->
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter Property="Foreground"
                                       Value="#CCCCCC"/>
                                <!-- 使用动画让悬浮内容淡入 -->
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverContent"
                                                            Storyboard.TargetProperty="Opacity"
                                                            To="1"
                                                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverContent"
                                                            Storyboard.TargetProperty="Opacity"
                                                            To="0"
                                                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="IconButtonStyle"
              TargetType="Button">
            <Setter Property="Background"
                   Value="Transparent"/>
            <Setter Property="Foreground"
                   Value="#CCCCCC"/>
            <Setter Property="BorderThickness"
                   Value="0"/>
            <Setter Property="Width"
                   Value="32"/>
            <Setter Property="Height"
                   Value="32"/>
            <Setter Property="FontSize"
                   Value="18"/>
            <Setter Property="FontFamily"
                   Value="Segoe MDL2 Assets"/>
            <Setter Property="Cursor"
                   Value="Hand"/>
            <Setter Property="Margin"
                   Value="0,0,1,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- 我们需要一个模板来确保背景真正受我们控制 -->
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                        Value="True">
                    <Setter Property="Foreground"
                           Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style x:Key="CustomTabItemStyle"
              TargetType="{x:Type TabItem}">
            <Setter Property="Width"
                   Value="70"/>
            <Setter Property="Foreground"
                   Value="#888888"/>
            <Setter Property="FontSize"
                   Value="15"/>
            <Setter Property="Padding"
                   Value="10,10,10,10"/>
            <Setter Property="Margin"
                   Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid HorizontalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <!-- 内容行 -->
                                <RowDefinition Height="Auto"/>
                                <!-- 指示器行 -->
                            </Grid.RowDefinitions>

                            <!-- 头部内容 -->
                            <ContentPresenter x:Name="ContentSite"
                                             Grid.Row="0"
                                             ContentSource="Header"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>

                            <!-- 选中时的红色下划线指示器 -->
                            <Border x:Name="SelectionIndicator"
                                   Grid.Row="1"
                                   Background="#EC4141"
                                   Height="3"
                                   Margin="0,0,0,0"
                                   Visibility="Collapsed"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected"
                                    Value="True">
                                <Setter Property="Foreground"
                                       Value="White"/>
                                <Setter Property="FontWeight"
                                       Value="Bold"/>
                                <Setter TargetName="SelectionIndicator"
                                       Property="Visibility"
                                       Value="Visible"/>
                            </Trigger>

                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected"
                                              Value="False"/>
                                    <Condition Property="IsMouseOver"
                                              Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Foreground"
                                       Value="#BBBBBB"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomTabControlStyle"
              TargetType="{x:Type TabControl}">
            <Setter Property="BorderThickness"
                   Value="0"/>
            <Setter Property="Background"
                   Value="Transparent"/>
            <Setter Property="Padding"
                   Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ModernMenuItemStyle"
              TargetType="MenuItem">
            <Setter Property="Background"
                   Value="Transparent"/>
            <Setter Property="Foreground"
                   Value="#CCCCCC"/>
            <Setter Property="Padding"
                   Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="border"
                               Background="{TemplateBinding Background}"
                               Padding="{TemplateBinding Padding}"
                               SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- IconPresenter 用于显示 MenuItem 的 Icon -->
                                <ContentPresenter x:Name="Icon"
                                                 Grid.Column="0"
                                                 Content="{TemplateBinding Icon}"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,10,0"/>
                                <!-- Header (文字) -->
                                <ContentPresenter Grid.Column="1"
                                                 ContentSource="Header"
                                                 RecognizesAccessKey="True"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- 鼠标悬浮时高亮 -->
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                       Property="Background"
                                       Value="#3F3F3F"/>
                                <Setter Property="Foreground"
                                       Value="AntiqueWhite"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernContextMenuStyle"
              TargetType="ContextMenu">
            <Setter Property="Background"
                   Value="#2D2D30"/>
            <Setter Property="BorderBrush"
                   Value="#444"/>
            <Setter Property="BorderThickness"
                   Value="1"/>
            <!-- 将我们创建的 MenuItem 样式应用到其所有子项 -->
            <Setter Property="ItemContainerStyle"
                   Value="{StaticResource ModernMenuItemStyle}"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="编辑歌单信息" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,30"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <!-- 左侧表单 -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- 右侧封面 -->
                </Grid.ColumnDefinitions>

                <!-- 左侧表单 -->
                <Grid Grid.Column="0" Margin="0,0,40,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 名称 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="名称:" Margin="0,10,20,0"/>
                    <Grid Grid.Row="0" Grid.Column="1">
                        <TextBox Text="{Binding Name}"/>
                        <TextBlock Text="{Binding NameCharCount}" IsHitTestVisible="False"
                                   Foreground="#888" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
                    </Grid>

                    <!-- 简介 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="简介:" Margin="0,25,20,0"/>
                    <Grid Grid.Row="1" Grid.Column="1" Margin="0,20,0,0">
                        <TextBox  Text="{Binding Description}"
                                  Height="150" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                        <TextBlock Text="{Binding DescriptionCharCount}" IsHitTestVisible="False"
                                   Foreground="#888" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
                    </Grid>

                    <!-- 标签 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="标签:" Margin="0,25,20,0"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,20,0,0"
                              ItemsSource="{Binding AvailableTags}" SelectedItem="{Binding SelectedTag}"
                              DisplayMemberPath="Name"
                              VerticalAlignment="Center"/>

                    <!-- 按钮组 -->
                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,40,0,0">
                        <Button Content="保存" Command="{Binding SaveCommand}" Width="100" Height="40" Margin="0,0,20,0" Style="{StaticResource PlayAllButtonStyle}"/>
                        <Button Content="取消" Command="{Binding CancelCommand}" Width="100" Height="40" Style="{StaticResource ActionButtonStyle}"/>
                    </StackPanel>
                </Grid>

                <!-- 右侧封面 -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <Border Width="200" Height="200" CornerRadius="8">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding CoverImageUrl,FallbackValue={StaticResource DefaultAvatarImage}}" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <Button Content="更换封面" Command="{Binding ChangeCoverCommand}" Margin="0,20,0,0" Style="{StaticResource ActionButtonStyle}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
