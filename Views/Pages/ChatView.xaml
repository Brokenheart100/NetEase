<UserControl x:Class="NetEase.Views.Pages.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NetEase.Views.Pages"
             xmlns:viewmodels="clr-namespace:NetEase.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ChatViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Background="White"
          ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- 第一栏：会话列表 -->
            <ColumnDefinition Width="*"/>
            <!-- 第二栏：聊天窗口 -->
        </Grid.ColumnDefinitions>

        <!-- ======================= 第一栏：会话列表 ======================= -->
        <Border Grid.Column="0"
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBox DockPanel.Dock="Top"
                         Text="搜索"
                         Margin="10"
                         Padding="5"/>
                <ListBox ItemsSource="{Binding Sessions}"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0"
                                         Width="40"
                                         Height="40"
                                         Margin="0,0,10,0">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Name}"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- ======================= 第二栏：聊天窗口 ======================= -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- 标题栏 -->
                <RowDefinition Height="*"/>
                <!-- 消息列表 -->
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0"
                    Padding="10"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="USUSUSUS"
                               FontSize="16"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="&#xE7C3;"
                                FontFamily="Segoe MDL2 Assets"
                                Margin="5,0"/>
                        <Button Content="&#xE713;"
                                FontFamily="Segoe MDL2 Assets"
                                Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Grid Grid.Row="1"
                  ShowGridLines="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" ShowGridLines="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ScrollViewer Grid.Row="0"
                                  Padding="10">
                        <ItemsControl ItemsSource="{Binding Messages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- 根 Grid，负责整体左右对齐 -->
                                    <Grid Margin="0,10" ShowGridLines="True">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="HorizontalAlignment"
                                                        Value="Left"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSentByMe}"
                                                                 Value="True">
                                                        <Setter Property="HorizontalAlignment"
                                                                Value="Right"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid ShowGridLines="True">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- 1. 别人的头像 (始终在第 0 列) -->
                                            <Ellipse Grid.Column="0"
                                                     Width="32"
                                                     Height="32"
                                                     Margin="0,0,10,0"
                                                     VerticalAlignment="Top">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSentByMe}"
                                                                         Value="True">
                                                                <Setter Property="Visibility"
                                                                        Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                                                </Ellipse.Fill>
                                            </Ellipse>

                                            <!-- 2. 消息内容 (StackPanel) -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                                             

                                                <!-- 发送者名称 -->
                                                <TextBlock Text="{Binding SenderName}"
                                                           Foreground="Gray"
                                                           FontSize="12">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="HorizontalAlignment"
                                                                    Value="Left"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSentByMe}"
                                                                             Value="True">
                                                                    <Setter Property="HorizontalAlignment"
                                                                            Value="Right"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!-- 消息气泡 -->
                                                <Border CornerRadius="8"
                                                        Padding="12"
                                                        Margin="0,5,0,0"
                                                        MaxWidth="400">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background"
                                                                    Value="#EEE"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSentByMe}"
                                                                             Value="True">
                                                                    <Setter Property="Background"
                                                                            Value="#007BFF"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Content}"
                                                               TextWrapping="Wrap">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground"
                                                                        Value="Black"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsSentByMe}"
                                                                                 Value="True">
                                                                        <Setter Property="Foreground"
                                                                                Value="White"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>
                                            </StackPanel>

                                            <!-- 3. 我方的头像 (始终在第 1 列) -->
                                            <Ellipse Grid.Column="2"
                                                     Width="32"
                                                     Height="32"
                                                     Margin="10,0,0,0"
                                                     VerticalAlignment="Top">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Visibility"
                                                                Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSentByMe}"
                                                                         Value="True">
                                                                <Setter Property="Visibility"
                                                                        Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                                                </Ellipse.Fill>
                                            </Ellipse>

                                        </Grid>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Row="1"
                            Padding="10"
                            BorderBrush="#E0E0E0"
                            BorderThickness="0,1,0,0">
                        <Grid>
                            <TextBox Text="输入消息..."
                                     VerticalAlignment="Stretch"
                                     Height="80"/>
                            <Button Content="发送(S)"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- 标题栏 -->
                        <RowDefinition Height="*"/>
                        <!-- 消息列表 -->
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0"
                                Margin="15">
                        <TextBlock Text="群公告"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="APEX 账号大全..."
                                   Margin="0,5,0,20"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1"
                                Margin="15">
                        <TextBlock Text="群成员 (3/12)"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10"/>
                        <ListBox ItemsSource="{Binding Members}"
                                 BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Grid.Column="0"
                                                 Width="24"
                                                 Height="24"
                                                 Margin="0,0,10,0">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
